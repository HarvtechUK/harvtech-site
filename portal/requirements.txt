# Python dependencies for the portal app.
#
# Install them into a virtual environment with:
#     pip install -r requirements.txt
#
# The version ranges below mean "any compatible release in this line".
# Once you're happy, `pip freeze > requirements.lock.txt` records the
# EXACT versions you tested with, so a deploy can't drift underneath you.

fastapi>=0.115,<1.0           # the web framework
uvicorn[standard]>=0.30,<1.0  # the ASGI server that actually runs the app
jinja2>=3.1,<4                # HTML templating for the server-rendered UI
azure-cosmos>=4.7,<5          # Cosmos DB SDK (the data layer's real backend)
azure-identity>=1.17,<2       # DefaultAzureCredential — keyless auth to Azure
# (pydantic comes bundled with fastapi — no need to list it separately yet)
