# This empty file marks the `app` directory as a Python "package", which
# is what lets us write imports like `from app.models import Engagement`
# and `from . import sample_data`. Python treats any directory with an
# __init__.py as an importable package.
